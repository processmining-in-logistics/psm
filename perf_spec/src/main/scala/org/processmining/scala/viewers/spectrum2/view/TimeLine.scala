package org.processmining.scala.viewers.spectrum2.view

import scala.swing.{BorderPanel, Dimension, Graphics2D}

class TimeLine extends BorderPanel{
  preferredSize = new Dimension(100, 1)

  override def paint(g: Graphics2D): Unit = {
    super.paint(g)

  }
}
